1|# QRkuy Evolution Log 🧬

Auto-generated by self-evolve cronjob. Setiap perubahan tercatat herein.

## [2026-08-01] QRIS Valid EMVCo + vCard & Event Fix
- **QRIS payload diperbaiki total** — sebelumnya TLV hardcode & invalid (length field salah, currency `604` bukan `360`, country ID hilang, CRC overflow 32-bit)
- **Proper EMVCo TLV encoder** — `tlv()` otomatis hitung length; struktur: 00 PFI, 01 PoI (11/12), 26 Merchant Account Info (00 GUID `ID` + 01 merchant PAN), 52 Category `0000`, 53 Currency `360` (IDR), 54 Amount (2-desimal sen), 58 Country `ID`, 59 Merchant Name, 60 City, 63 CRC
- **CRC16/XModem fixed** — mask `& 0xFFFF` tiap step; hasil verified cross-check python (0C39 == 0C39)
- **vCard escape fixed** — escape `,` `;` `\n` sekarang benar (sebelumnya double-backslash di runtime)
- **Event template** — field Mulai/Selesai ganti ke `datetime-local` input (date picker native, bukan text manual)
- Build passes — TypeScript clean, Vite build successful

## [2026-07-30] Background Image QR
- **Background image** — user bisa upload gambar sebagai latar belakang QR code
- **Fitur**: upload gambar, otomatis di-crop cover-fit ke seluruh QR area
- **Semi-transparent overlay** — 80% opacity bg color overlay agar QR tetap readable
- **Disimpan di history** — config bgImage Restoration saat pick dari riwayat
- **UI baru** — "Latar Belakang" card di Generate tab dengan upload button
- Build passes — TypeScript clean, Vite build successful

## [2026-07-30] URL Metadata Preview
- **URL metadata preview** — baru: favicon + domain name otomatis muncul saat user input URL
- **Fitur**: ambil favicon via Google favicon service, tampilkan di bawah input bar
- **Use case**: user tahu URL yang diinput valid tanpa harus membuka link
- Push ke main — Vercel auto-deploy
- **QRIS template** — baru: generate QRIS payment QR codes untuk merchant Indonesia
- **Fields**: Merchant ID (required), Jumlah (optional), Nama Merchant (optional)
- **Format**: QRIS MPM Static dengan CRC16 checksum
- **Use case**: merchant generate QR buat diterima payment via bank apps/e-wallet
- Build passes — TypeScript clean, Vite build successful

## [2026-07-30] Custom Filename + Clipboard Paste Scan
- **Custom filename input** — user bisa isi nama file kustom sebelum download (PNG/SVG/PDF), max 40 chars
- **Filename preview** di placeholder input shows default name yang akan dipakai
- **Filename affects semua export**: download PNG, SVG, PDF, share, bahkan Web Share API
- **Paste dari Clipboard button** di Scan tab — langsung scan QR dari gambar di clipboard tanpa upload manual
- **Requires clipboard read permission** — browser akan minta izin first time use
- Build passes — TypeScript clean, Vite build successful

## [2026-07-30] Tema Siap Pakai — One-Click QR Theme Presets
6|- **10 curated themes** — Brutalist, Retro Pink, Ocean Blue, Forest, Neon Nights, Sunset, Monochrome, Coffee, Cyberpunk, Royal
7|- **One-click apply** — each theme sets fg, bg, dot style, gradient, frame, and heart in a single click
8|- **Visual picker** — 2×N grid with gradient swatch + label + description for each theme
9|- **Dark mode compatible** — themes section respects dark mode styling
10|- Works with existing auto-gen debounce — click theme, QR regenerates automatically
11|- Build passes — TypeScript clean, Vite build successful
12|
13|## [2026-07-30] Debounced Auto-Generate QR + Gen Counter
- **Auto-generate on change** — QR regenerates automatically after 600ms idle when text/colors/style/size/logo change. No more manual "Buat QR Code" click.
- **Clears debounce on manual gen** — clicking "Buat QR Code" instantly cancels pending auto-gen and runs immediately
- **Gen counter** — tracks total QRs generated (persisted in localStorage), shown in footer
- **genFnRef pattern** — avoids stale closure issues from debounced callbacks
- **Skips first auto-gen** — URL param auto-gen still fires once on load without debounce
- Build passes — TypeScript clean, Vite build successful

## [2026-07-29] Custom Text Logo in QR Center
- **Added text logo** — users can type up to 8 chars (brand/initials) displayed in QR center
- **3 modes now**: Heart ❤️, Upload Image, or custom Teks
- **Text auto-scales** to fit circular center area, crisp bold sans-serif
- **SVG/PDF export** renders text logo with SVG `<text>` element (no raster fallback)
- **Shareable URL** — `?logo=` param carries text logo in QR link
- **History** saves & restores text logo config
- **Batch generate** respects text logo setting
- Text logo takes priority over heart/image when set
- Build passes — TypeScript clean, Vite build successful

---

## [2026-07-29] Full Config History & Shareable QR URL
- **History now saves full QR config** — colors, dot style, size, ECC, gradient, frame, heart — not just text
- **pickHistory restores all settings** — click history item, get exact same QR with all styling
- **Shareable URL encodes all config params** — `?text=&fg=&bg=&style=&size=&ecc=&grad=&frame=&heart=`
- **New page load parses URL params** — share a link, recipient sees exact same styled QR
- **"Salin Link QR" button now uses full URL** with all settings, not just `?text=`
- HistoryTab type updated to accept wider HistoryEntry interface
- Build passes — TypeScript clean, Vite build successful

---

## [2026-07-29] Batch Export as PDF
- **Added batch export as single PDF** — users can now export multiple QR codes as one PDF document (grid layout, A4 format)
- **Format selector in batch mode** — ZIP (individual PNGs) or PDF (all-in-one document)
- **Smart grid layout** — 2 cols for ≤4 QRs, 3 cols for ≤9, 4 cols for more, auto page breaks
- **Includes text labels** under each QR in PDF for easy identification
- **Respects all QR settings** — gradient, framed, dot style, colors all carry over to batch PDF
- Build passes — TypeScript clean, Vite build successful

---

## [2026-07-29] Dark Mode Toggle
- **Added dark mode toggle** in header — Moon/Sun icon button, persists to localStorage (`qrkuy-dark-mode`)
- **Detects system preference** on first load via `prefers-color-scheme: dark`
- **Updated all UI components** with dark variants — cards, inputs, buttons, tabs, selects, toggles
- **Preserved brutalist aesthetic** — uses `#1d3557` (navy) as dark shadow color, `#0f0f0f` background
- **Card component** updated with dark bg (`#1a1a1a`) and white borders/shadows
- Build passes — TypeScript clean, Vite build successful

---

## [2026-07-28] Print QR Feature
- **Added Print button** in Generate tab — triggers `window.print()` to print QR directly
- **Added Print button in Template tab** — appears only when text/QR exists
- **Added print styles** in index.css — hides header/footer/buttons during print, shows only QR content
- Uses `Printer` icon from lucide-react
- Build passes — TypeScript clean, Vite build successful

## [2026-07-28] Recent Colors Memory
- **Added `useRecentColors` hook** — saves last 8 used color pairs (fg|bg) to localStorage (`qrkuy-recent-colors`)
- **Enhanced color section UI** — displays "Warna Terakhir" section with quick-access buttons to recently used color combinations
- **Auto-save on QR generation** — colors are saved to recent list each time user generates a QR code
- **Clear recent colors button** — allows user to wipe recent colors history
- **Type-safe localStorage hook** — updated `useLocalStorage` to support functional updates (like React's useState)
- **Build passes** — TypeScript clean, Vite build successful

## [2026-07-28] Fix ScanTab Type Error & Add Image Scan
- **Fixed TypeScript error** — `ScanTabProps` missing `imageScanning`, `setImageScanning`, `handleImageScan`, `clearScannedText` props passed from App.tsx (build was failing with TS2739)
- **Added `handleImageScan`** in App.tsx using `BarcodeDetector` API + `jsQR` fallback for scanning QR from uploaded images
- **Added `clearScannedText`** to clear scan result/error
- **Enhanced ScanTab UI** — added "Upload Gambar" button with loading spinner, file input accepts images, clear (X) button on scanned result
- **Added `jsqr` dependency** for client-side QR decoding fallback when native BarcodeDetector fails
- **Build passes** — TypeScript clean, Vite build successful

## [2026-07-28] Clean Build & Dependabot-Quality Fixes
- **Removed dead shadcn component wrappers** — `Button`, `Input`, `Label`, `Select` deleted (unused since brutalist restyle). Only `Card` components remain.
- **Removed premature camera permission prompt** — `getUserMedia` on mount asked camera permission before user even clicked Scan tab. Now only requested on `startScanning()`.
- **Added SVG gradient support** — `exportSVG` now accepts `gradientEnabled`/`gradientTo` params and renders SVG `<linearGradient>` defs instead of flat color. Fixes gradient loss on SVG/PDF export.
- **Fixed INEFFECTIVE_DYNAMIC_IMPORT** — `export.ts` now statically imports `drawQR` instead of 3 separate dynamic imports. Main chunk avoids unnecessary module splitting. 0 build warnings.
- **Added service worker** — basic `public/sw.js` for offline cache-first of static assets.
- **Removed unused `hasCameraPermission` state** — simplified ScanTab interface.
- **Removed unused `useCallback` import** in App.tsx.

## [2026-07-26] Initial setup
- React + TypeScript + Vite scaffold
- shadcn/ui components: Card, Button, Input, Label, Select
- Tailwind v4 + neo-brutalism theme
- Lucide icons

## [2026-07-26] QR Generation
- QR code generator via qrcode-generator library
- Custom colors (8 presets + custom picker)
- Dot styles: Rounded, Circle, Square
- Size options: 200/300/400/500px
- Heart logo center toggle
- Custom image upload for center logo

## [2026-07-26] Features
- History tab (localStorage, 20 items)
- Download PNG and copy to clipboard
- Share using Web Share API
- Copy text
- Gradient QR color support
- ECC Level (L/M/Q/H)

## [2026-07-26] UI/UX
- Neo-brutalism design (thick borders, colored shadows, no radius)
- Responsive grid layout (mobile + desktop)
- Tab navigation
- Bold uppercase typography
- Brutalist color palette

## [2026-07-27] SEO & UX Enhancement
- OG meta tags, Twitter cards, JSON-LD schema for rich link previews
- Canonical URL added
- Custom favicon link (favicon.svg)
- URL search param support (`?text=`) — shareable QR links
- Auto-generate QR when text pre-filled from URL
- Clear text button (XCircle) inside input field
- Custom brutalist scrollbar styling
- Yellow-on-black selection highlight
- Fade-up animation on QR result card
- Better alt text for QR images (descriptive)
- aria-label on icon-only buttons for accessibility

## [2026-07-27] Performance & Features
- Lazy-load HistoryTab via React.lazy + Suspense (1.6KB code-split)
- Brutalist QR frame toggle (4-corner yellow brackets + thick border)
- Character count indicator + URL auto-detection badge
- "Salin Link QR" button — copy shareable URL with `?text=` param
- Web App Manifest (manifest.json) for PWA-like installability

## [2026-07-27] PWA, Search History & Clipboard Shortcut
- Service Worker (`sw.js`) with network-first caching strategy for offline PWA support
- Register SW on page load — enables browser install prompt
- Search/filter bar in History tab — real-time filtering with clear button
- "Tidak ditemukan" state when search yields no results
- "Tempel dari clipboard" button in text input — paste clipboard content directly
- Added `ClipboardPasteIcon` to input action buttons

## [2026-07-27] UX & Productivity
- Updated README.md with comprehensive documentation
- Added global keyboard shortcut: `Ctrl+K` / `Cmd+K` to focus text input
- Added export History as `json` button in History tab

## [2026-07-27] Bug Fix & Type Safety
- Fixed broken JSX structure in App.tsx (extra closing `</div>` causing build failure)
- Added `template` to `Tab` type union for proper type safety
- Added `step` property to `TemplateField` interface for number input fields (e.g., geo coordinates)
- Build now passes with `npm run build` (tsc + vite)

## [2026-07-27] Export & Accessibility Features
- Added **Export as SVG** button — vector format for infinite scaling
- Added **Export as PDF** button — print-ready QR codes
- Added **WCAG AA Accessibility Checker** — real-time contrast ratio validation (4.5:1 minimum)
  - Shows green badge when contrast passes, red with issues when fails
  - Checks color contrast ratio, minimum QR size (200px), maximum size (1000px)
  - Runs automatically on QR generation
- Added **Batch QR Generation** in Template tab — paste multiple lines, generate all as ZIP
  - Uses JSZip to package multiple PNGs into single download
  - Progress bar with percentage during generation
  - One QR per line from text/CSV input
- Imported `jszip` for ZIP packaging
- Added `downloadBatchAsZip`, `downloadSVG`, `downloadPDF` to export.ts

## [2026-07-28] Scan Tab & Template Tab Fix
- **Added Scan Tab UI** — `ScanTab.tsx` component with camera view, scan overlay, torch toggle, scanned result display, copy & use-scanned-text buttons
- **Added Template Tab render** — previously selected but rendered nothing; now renders `TemplateTab` with all QR options passed through
- **Brutalist restyle** — `TemplateTab` UI converted from shadcn/indigo theme to consistent yellow/black/pink/blue brutalist design
- **Passed QR options** to TemplateTab: `dotStyle`, `eccLevel`, `gradientEnabled`, `gradientTo`, `framed`, `qrSize` — batch generation now uses actual user settings instead of hardcoded defaults
- **Lazy-loaded ScanTab** via `React.lazy` + Suspense for consistent code-splitting

## [2026-07-28] Real PDF Export, Brutalist TemplateTab, Individual History Delete
- **Real PDF export** — replaced fake HTML-as-PDF with `jspdf` library; SVG rendered to canvas then embedded in genuine PDF
- **Batch gen now respects all options** — `gradientEnabled`, `gradientTo`, `framed` passed to `downloadBatchAsZip`
- **Fully brutalist TemplateTab** — removed remaining shadcn components (`Button`, `Input`, `Label`, `Select`), replaced with raw HTML consistent with brutalist design
- **Individual history item delete** — each history row now has an X button to delete single items instead of only nuke-all
- Added `jspdf` and `@types/jspdf` dependencies

## [2026-07-28] Bug Fix: PDF Orientation, Stale Closure, Redundant Dynamic Imports
- **Fixed PDF orientation** — `qrSize > qrSize` always false; now uses `qrSize > 500` for landscape
- **Fixed stale closure** — `handleBatchGenerate` missing `gradientEnabled`, `gradientTo`, `framed` deps in `useCallback`
- **Removed redundant dynamic imports** — `exportSVG` and `checkAccessibility` were already statically imported; removed inline `await import()` calls eliminating 2 `INEFFECTIVE_DYNAMIC_IMPORT` warnings
- **TemplateTab chunk size dropped** 15.41kB → 9.51kB from dynamic import cleanup
- **Added Color-Blind Safe Palettes** — quick-access buttons from `COLOR_BLIND_PALETTES` in Colors section for WCAG AA compliance


## [2026-08-01] Corner Eye Styles, Transparent BG, QR Capacity Meter, 5 New Templates, CSV Batch
- **Corner eye styles** — `cornerStyle` (square/rounded/dot/extra-rounded) dengan `drawCornerEye` di `qr.ts`
- **Transparent background** — toggle `transparentBg` (transparency pixel probe di canvas)
- **Quiet zone adjustable** — `quietZone` (0–10 module) via URL param `quiet`
- **QR capacity meter** — `getQRMeta()` menampilkan versi QR, % kapasitas terpakai, kapasitas max per ECC level, progress bar visual
- **5 template baru** — MeCard, PayPal Payment, UPI (India), Bluetooth Pairing, App Store/Play Store
- **Kategori Pembayaran** baru di TemplateTab — QRIS/Crypto/Lightning/PayPal/UPI dikelompokkan terpisah
- **CSV upload** di batch generation — upload file CSV langsung isi batch input
- URL params baru: `corner`, `transp`, `quiet` (parse + buildShareURL sinkron)

## [2026-08-01] Docs: README & EVOLUTION
- **README proper** — ganti template default `roast-my-code-action` dengan dokumentasi lengkap QRkuy (fitur, tech stack, cara pakai, tabel URL params, struktur project)
- **EVOLUTION.md** — catat sesi corner styles + templates + CSV
